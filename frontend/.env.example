# The frontend only calls same-origin routes. In local development, Vite proxies
# those routes to this backend target; this variable is not bundled into the app.
HARNESS_BACKEND_PROXY_TARGET=http://127.0.0.1:11431
